import ScreenFixed from '@/components/screens/screen-fixed';

export default function Page() {
  return (
    <div style={{ position: 'relative', minHeight: '100dvh' }}>
      <ScreenFixed />
    </div>
  );
}
