import ScreenInstall from '@/components/screens/screen-install';

export default function Page() {
  return (
    <div style={{ position: 'relative', minHeight: '100dvh' }}>
      <ScreenInstall />
    </div>
  );
}
